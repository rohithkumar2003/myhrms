import { createContext } from 'react';

// Create and export the context for the settings module.
// This will be used by the provider and the UI components to share data.
export const SettingsContext = createContext(null);